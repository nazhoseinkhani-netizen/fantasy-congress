---
phase: 05-polish-and-viral
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dev-mode/dev-mode-provider.tsx
  - src/components/dev-mode/dev-mode-banner.tsx
  - src/styles/globals.css
  - src/components/layout/root-layout.tsx
  - src/components/feed/trade-card.tsx
  - src/components/dashboard/kpi-row.tsx
  - src/components/dashboard/matchup-scoreboard.tsx
  - src/components/leaderboard/shame-table.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [PLAT-03]

must_haves:
  truths:
    - "Pressing Ctrl+Shift+D toggles Developer Mode on and off"
    - "When Developer Mode is active, a banner shows at the top of the page confirming the mode"
    - "When Developer Mode is active, data elements with data-alva-skill attributes show dashed borders and tooltips identifying the Alva Skill"
    - "data-alva-skill attributes are always in the DOM (not conditional) to avoid hydration mismatch"
  artifacts:
    - path: "src/components/dev-mode/dev-mode-provider.tsx"
      provides: "React context for dev mode state + keydown listener"
      exports: ["DevModeProvider", "useDevMode"]
    - path: "src/components/dev-mode/dev-mode-banner.tsx"
      provides: "Top-of-page banner when dev mode is active"
      exports: ["DevModeBanner"]
    - path: "src/styles/globals.css"
      provides: "CSS rules for .dev-mode [data-alva-skill] dashed borders"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/dev-mode/dev-mode-provider.tsx"
      via: "DevModeProvider wraps children in RootLayout"
      pattern: "<DevModeProvider>"
    - from: "src/styles/globals.css"
      to: "data-alva-skill attributes"
      via: "CSS selector .dev-mode [data-alva-skill]"
      pattern: "\\.dev-mode \\[data-alva-skill\\]"
---

<objective>
Implement the Developer Mode Easter egg system: Ctrl+Shift+D keyboard shortcut toggles dev mode, which reveals dashed borders and tooltips on data elements showing which Alva Skill powers each piece of data.

Purpose: PLAT-03 requires a developer mode toggle showing which Alva Skill powers each data element with dashed borders and tooltips. This is the "lift the hood on a race car" wow moment.

Output: DevModeProvider context, DevModeBanner component, CSS rules for dev mode visuals, data-alva-skill attributes on key data elements across the app.
</objective>

<execution_context>
@/Users/solofilms/.claude/get-shit-done/workflows/execute-plan.md
@/Users/solofilms/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-viral/05-RESEARCH.md

<interfaces>
From src/app/layout.tsx:
```typescript
// Root app layout — wraps all pages
export default function Layout({ children }: { children: React.ReactNode })
```

From src/components/layout/root-layout.tsx:
```typescript
export function RootLayout({ children }: RootLayoutProps)
// NOTE: Plan 01 modifies this file too (adds AlvaFooter). Plan 02 adds DevModeBanner.
// Both plans are Wave 1 (parallel). Executor must merge both changes.
// Plan 02 adds: DevModeBanner import + render inside main.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DevModeProvider context and DevModeBanner component</name>
  <files>src/components/dev-mode/dev-mode-provider.tsx, src/components/dev-mode/dev-mode-banner.tsx, src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (current CSS to append dev mode rules)
    - src/app/layout.tsx (where DevModeProvider will wrap)
    - .planning/phases/05-polish-and-viral/05-RESEARCH.md (Pattern 5 code example)
  </read_first>
  <action>
**Create src/components/dev-mode/dev-mode-provider.tsx:**
- `'use client'` directive
- Import `createContext, useContext, useState, useEffect, useCallback` from `'react'`
- Interface `DevModeContextValue { devMode: boolean; toggle: () => void }`
- Create `DevModeContext` with `createContext<DevModeContextValue>({ devMode: false, toggle: () => {} })`
- Export `DevModeProvider` component wrapping children in context provider
- `const [devMode, setDevMode] = useState(false)`
- `const toggle = useCallback(() => setDevMode((v) => !v), [])`
- `useEffect` with `keydown` handler: check `e.ctrlKey && e.shiftKey && e.key === 'D'` — call `e.preventDefault()` then `toggle()`. Add listener on `document`, remove on cleanup. Per Pitfall 5: `e.preventDefault()` avoids browser DevTools conflict.
- `useEffect` for `document.documentElement.classList.toggle('dev-mode', devMode)` — syncs CSS class
- Export `useDevMode` hook: `() => useContext(DevModeContext)`

**Create src/components/dev-mode/dev-mode-banner.tsx:**
- `'use client'` directive
- Import `useDevMode` from `'./dev-mode-provider'`
- Import `{ motion, AnimatePresence }` from `'motion/react'`
- Import `{ X }` from `'lucide-react'`
- Export `DevModeBanner` component
- Render only when `devMode` is true, wrapped in `<AnimatePresence>`
- `<motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ type: 'spring', stiffness: 300, damping: 30 }}>`
- Inner div: `className="bg-primary/10 border-b border-primary/30 px-4 py-2 flex items-center justify-between"`
- Left side: `<div className="flex items-center gap-2"><span className="inline-block w-2 h-2 rounded-full bg-primary animate-pulse" /><span className="text-xs font-mono text-primary">Developer Mode Active</span></div>`
- Right side: `<div className="flex items-center gap-3"><span className="text-xs text-muted-foreground font-mono">Data elements highlighted — hover for Alva Skill attribution</span><button onClick={toggle} className="text-muted-foreground hover:text-foreground"><X className="w-3.5 h-3.5" /></button></div>`

**Append to src/styles/globals.css** (after the existing `@layer base` block):
```css
/* Developer Mode — data attribution borders */
.dev-mode [data-alva-skill] {
  outline: 2px dashed oklch(0.78 0.165 85 / 0.5);
  outline-offset: 2px;
  position: relative;
  cursor: help;
}

.dev-mode [data-alva-skill]:hover::after {
  content: 'Alva Skill: ' attr(data-alva-skill);
  position: absolute;
  bottom: 100%;
  left: 50%;
  transform: translateX(-50%);
  background: oklch(0.115 0.008 265);
  border: 1px solid oklch(0.78 0.165 85 / 0.4);
  color: oklch(0.78 0.165 85);
  font-family: var(--font-mono);
  font-size: 11px;
  padding: 4px 8px;
  border-radius: 4px;
  white-space: nowrap;
  z-index: 50;
  pointer-events: none;
}
```
  </action>
  <verify>
    <automated>cd /Users/solofilms/FantasyCongress && test -f src/components/dev-mode/dev-mode-provider.tsx && test -f src/components/dev-mode/dev-mode-banner.tsx && grep -q "ctrlKey.*shiftKey" src/components/dev-mode/dev-mode-provider.tsx && grep -q "preventDefault" src/components/dev-mode/dev-mode-provider.tsx && grep -q "dev-mode" src/styles/globals.css && grep -q "data-alva-skill" src/styles/globals.css && grep -q "Developer Mode Active" src/components/dev-mode/dev-mode-banner.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dev-mode/dev-mode-provider.tsx contains `export function DevModeProvider`
    - src/components/dev-mode/dev-mode-provider.tsx contains `export const useDevMode` or `export function useDevMode`
    - src/components/dev-mode/dev-mode-provider.tsx contains `e.ctrlKey && e.shiftKey && e.key === 'D'`
    - src/components/dev-mode/dev-mode-provider.tsx contains `e.preventDefault()`
    - src/components/dev-mode/dev-mode-provider.tsx contains `classList.toggle('dev-mode', devMode)`
    - src/components/dev-mode/dev-mode-banner.tsx contains `Developer Mode Active`
    - src/components/dev-mode/dev-mode-banner.tsx imports from `'motion/react'`
    - src/styles/globals.css contains `.dev-mode [data-alva-skill]` rule with `outline: 2px dashed`
    - src/styles/globals.css contains `.dev-mode [data-alva-skill]:hover::after` tooltip rule with `content: 'Alva Skill: ' attr(data-alva-skill)`
  </acceptance_criteria>
  <done>DevModeProvider and DevModeBanner components created. CSS rules for dev mode dashed borders and hover tooltips appended to globals.css.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DevModeProvider into app layout and add data-alva-skill attributes to key components</name>
  <files>src/app/layout.tsx, src/components/layout/root-layout.tsx, src/components/feed/trade-card.tsx, src/components/dashboard/kpi-row.tsx, src/components/dashboard/matchup-scoreboard.tsx, src/components/leaderboard/shame-table.tsx</files>
  <read_first>
    - src/app/layout.tsx (current app root layout)
    - src/components/layout/root-layout.tsx (current RootLayout — may already have AlvaFooter from Plan 01)
    - src/components/feed/trade-card.tsx (trade card component to add data-alva-skill)
    - src/components/dashboard/kpi-row.tsx (KPI component to add data-alva-skill)
    - src/components/dashboard/matchup-scoreboard.tsx (scoreboard to add data-alva-skill)
    - src/components/leaderboard/shame-table.tsx (leaderboard table to add data-alva-skill)
  </read_first>
  <action>
**Update src/app/layout.tsx:**
- Import `DevModeProvider` from `'@/components/dev-mode/dev-mode-provider'`
- Wrap the `{children}` (or `<RootLayout>{children}</RootLayout>` if that's the pattern) inside `<DevModeProvider>...</DevModeProvider>`
- DevModeProvider should wrap the outermost client boundary so all pages receive the context

**Update src/components/layout/root-layout.tsx:**
- Import `DevModeBanner` from `'@/components/dev-mode/dev-mode-banner'`
- Render `<DevModeBanner />` as the first child inside the `<>` fragment, before `<NavDesktop />`
- This places the banner at the very top of the viewport

**Add `data-alva-skill` attributes to data-driven components (always present in DOM per Pitfall 4 — NOT conditional on devMode):**

Per D-11, attribute the 3-4 main Alva Skills: `getSenatorTrades`, `getStockPrices`, `getPoliticianMetadata`.

1. **src/components/feed/trade-card.tsx**: Add `data-alva-skill="getSenatorTrades"` to the outermost container `<div>` or `<Card>` of each trade card. This is the trade disclosure data element.

2. **src/components/dashboard/kpi-row.tsx**: Add `data-alva-skill="getSenatorTrades"` to the KPI container div that shows season points and trade stats. These are derived from trade data.

3. **src/components/dashboard/matchup-scoreboard.tsx**: Add `data-alva-skill="getSenatorTrades"` to the scoreboard container. Scores are derived from trade scoring engine.

4. **src/components/leaderboard/shame-table.tsx**: Add `data-alva-skill="getSenatorTrades"` to each row's points/score cell, AND add `data-alva-skill="getPoliticianMetadata"` to the politician name/photo cell. The leaderboard has two data sources: trade-derived scores and politician metadata.
  </action>
  <verify>
    <automated>cd /Users/solofilms/FantasyCongress && grep -q "DevModeProvider" src/app/layout.tsx && grep -q "DevModeBanner" src/components/layout/root-layout.tsx && grep -q 'data-alva-skill' src/components/feed/trade-card.tsx && grep -q 'data-alva-skill' src/components/dashboard/kpi-row.tsx && grep -q 'data-alva-skill' src/components/leaderboard/shame-table.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains `<DevModeProvider>` wrapping children
    - src/app/layout.tsx imports `DevModeProvider` from `'@/components/dev-mode/dev-mode-provider'`
    - src/components/layout/root-layout.tsx contains `<DevModeBanner />`
    - src/components/layout/root-layout.tsx imports from `'@/components/dev-mode/dev-mode-banner'`
    - src/components/feed/trade-card.tsx contains `data-alva-skill="getSenatorTrades"`
    - src/components/dashboard/kpi-row.tsx contains `data-alva-skill="getSenatorTrades"`
    - src/components/dashboard/matchup-scoreboard.tsx contains `data-alva-skill="getSenatorTrades"`
    - src/components/leaderboard/shame-table.tsx contains `data-alva-skill=` (at least one attribution)
    - data-alva-skill attributes are NOT wrapped in any conditional (no `{devMode && ...}`)
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Developer Mode system fully wired: Ctrl+Shift+D toggles banner, dashed borders appear on trade cards, KPI rows, matchup scoreboard, and leaderboard rows with hover tooltips showing Alva Skill names. Attributes are always in the DOM (no hydration issues).</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- DevModeProvider wraps all pages via layout.tsx
- DevModeBanner renders at top of page when dev mode active
- data-alva-skill attributes present on trade card, KPI row, matchup scoreboard, and shame table elements
- CSS rules in globals.css target `.dev-mode [data-alva-skill]` with dashed borders and hover tooltips
</verification>

<success_criteria>
- Ctrl+Shift+D toggles Developer Mode on/off
- When active, a gold-tinted banner appears at top saying "Developer Mode Active"
- Data elements show dashed gold borders and hover tooltips with "Alva Skill: getSenatorTrades" (or appropriate skill name)
- Attributes are unconditionally in the DOM (no hydration mismatch)
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-viral/05-02-SUMMARY.md`
</output>
