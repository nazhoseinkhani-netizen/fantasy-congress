---
phase: 05-polish-and-viral
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/draft/draft-board.tsx
  - src/components/draft/pick-ticker.tsx
  - src/components/dashboard/kpi-row.tsx
  - src/components/dashboard/matchup-scoreboard.tsx
  - src/components/leaderboard/shame-table.tsx
  - src/components/leaderboard/leaderboard-page.tsx
  - src/components/animations/animated-gauge.tsx
  - src/app/leaderboard/page.tsx
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03, ANIM-05]

must_haves:
  truths:
    - "Draft picks animate with a fly-to-roster effect and burst when a pick is made"
    - "Trade cards in the feed and dashboard slide in from the right with green/red glow based on return"
    - "Points counters on dashboard KPIs and matchup scoreboard use the animated digit-flip ticker"
    - "The leaderboard page has an animated Swamp-o-meter gauge that fills on scroll-into-view"
  artifacts:
    - path: "src/components/draft/draft-board.tsx"
      provides: "Draft pick fly animation overlay on user/AI pick"
    - path: "src/components/dashboard/kpi-row.tsx"
      provides: "Animated digit-flip counters for KPI values"
    - path: "src/components/dashboard/matchup-scoreboard.tsx"
      provides: "Animated team scores with digit-flip"
    - path: "src/components/leaderboard/leaderboard-page.tsx"
      provides: "Swamp-o-meter animated gauge"
  key_links:
    - from: "src/components/dashboard/kpi-row.tsx"
      to: "src/components/animations/animated-counter.tsx"
      via: "import AnimatedCounter or DigitFlipCounter"
      pattern: "import.*AnimatedCounter.*from.*animations"
    - from: "src/components/leaderboard/leaderboard-page.tsx"
      to: "src/components/animations/animated-gauge.tsx"
      via: "import AnimatedGauge for swamp-o-meter"
      pattern: "import.*AnimatedGauge.*from.*animations"
---

<objective>
Wire animation primitives into existing pages: draft pick fly-to-roster animation, trade alert slide-ins with glow, scoreboard digit-flip counters on dashboard, and the Swamp-o-meter gauge on the leaderboard.

Purpose: This is where "the app feels alive" (D-01, D-02). Every major interaction and page load triggers visible, dramatic animation. Draft picks fly, scores tick up, gauges fill.

Output: Enhanced draft board with pick animation, animated KPI/matchup scores, slide-in trade cards, and Swamp-o-meter on leaderboard.
</objective>

<execution_context>
@/Users/solofilms/.claude/get-shit-done/workflows/execute-plan.md
@/Users/solofilms/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-viral/05-RESEARCH.md
@.planning/phases/05-polish-and-viral/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/components/animations/animated-counter.tsx:
```typescript
export function AnimatedCounter({ value, duration, className, formatFn }: {
  value: number; duration?: number; className?: string; formatFn?: (n: number) => string
}): JSX.Element

export function DigitFlipCounter({ value, className }: {
  value: number; className?: string
}): JSX.Element
```

From src/components/animations/animated-gauge.tsx:
```typescript
export function AnimatedGauge({ score, size, showLabel, labelText, animateOnMount, className }: {
  score: number; size?: 'sm' | 'md' | 'lg'; showLabel?: boolean;
  labelText?: string; animateOnMount?: boolean; className?: string
}): JSX.Element

export function getTierLabel(score: number): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add draft pick fly animation and trade alert slide-ins</name>
  <files>src/components/draft/draft-board.tsx, src/components/draft/pick-ticker.tsx</files>
  <read_first>
    - src/components/draft/draft-board.tsx (full file — understand pick flow and recordPick call)
    - src/components/draft/pick-ticker.tsx (existing AnimatePresence pattern for ticker entries)
    - src/components/draft/draft-roster.tsx (roster panel — target for fly-to animation)
    - src/components/draft/on-the-clock.tsx (existing motion patterns)
    - .planning/phases/05-polish-and-viral/05-RESEARCH.md (Pattern 1 and Code Example for fly-to-roster)
  </read_first>
  <action>
**ANIM-01: Draft pick fly animation in src/components/draft/draft-board.tsx:**
- Import `{ motion, AnimatePresence, useAnimate }` from `'motion/react'`
- Add state: `const [flyAnimation, setFlyAnimation] = useState<{ politicianName: string; fromRect: DOMRect } | null>(null)`
- Create a fixed-position overlay `<div>` at the end of the component (inside the return): `<AnimatePresence>{flyAnimation && <motion.div className="fixed z-50 pointer-events-none" style={{ left: flyAnimation.fromRect.left, top: flyAnimation.fromRect.top, width: flyAnimation.fromRect.width, height: flyAnimation.fromRect.height }} initial={{ scale: 1, opacity: 1 }} animate={{ x: [0, -50, -200], y: [0, -30, 100], scale: [1, 1.15, 0.3], opacity: [1, 1, 0] }} transition={{ duration: 0.6, ease: 'easeInOut' }} onAnimationComplete={() => setFlyAnimation(null)}><div className="bg-card border border-primary rounded-lg p-2 shadow-lg shadow-primary/20 text-sm font-semibold text-center truncate">{flyAnimation.politicianName}</div></motion.div>}</AnimatePresence>`
- Wrap the existing `recordPick` call (wherever user clicks to pick): before calling `recordPick(politicianId)`, capture the click target's `getBoundingClientRect()` and the politician name, set `setFlyAnimation({ politicianName, fromRect })`, then call `recordPick` after a 50ms `setTimeout` so the animation starts before the state update removes the card from the pool
- Add a burst effect: after the fly animation, render 6 small `<motion.div>` particles (2x2px gold circles) that scale from 0 to 1 and translate outward in random directions over 0.4s, fading to opacity 0 — these render at the end position of the fly animation

**ANIM-02: Trade alert slide-in in src/components/draft/pick-ticker.tsx:**
- The pick ticker already uses `AnimatePresence` — enhance each pick entry's motion props:
- Change `initial` to `{ opacity: 0, x: 60 }` (slide from right)
- Add conditional `style` prop: `boxShadow: pick was positive return ? '0 0 12px rgba(34,197,94,0.25)' : '0 0 12px rgba(239,68,68,0.25)'`
- If the pick ticker doesn't have return data, use a simple gold glow: `'0 0 12px rgba(234,179,8,0.2)'`
- Transition: `{ type: 'spring', stiffness: 300, damping: 28 }`
  </action>
  <verify>
    <automated>cd /Users/solofilms/FantasyCongress && grep -q "flyAnimation" src/components/draft/draft-board.tsx && grep -q "onAnimationComplete" src/components/draft/draft-board.tsx && grep -q "getBoundingClientRect" src/components/draft/draft-board.tsx && grep -q "boxShadow" src/components/draft/pick-ticker.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/draft/draft-board.tsx contains `flyAnimation` state
    - src/components/draft/draft-board.tsx contains `getBoundingClientRect` for capturing pick source position
    - src/components/draft/draft-board.tsx contains `onAnimationComplete={() => setFlyAnimation(null)}`
    - src/components/draft/draft-board.tsx contains a fixed-position overlay div with `className="fixed z-50 pointer-events-none"`
    - src/components/draft/draft-board.tsx contains burst particle elements (at least `motion.div` with scale animation)
    - src/components/draft/pick-ticker.tsx contains `boxShadow` with green (`34,197,94`) or gold glow
    - src/components/draft/pick-ticker.tsx contains `x: 60` in initial prop (slide from right)
    - src/components/draft/pick-ticker.tsx contains spring transition with `stiffness: 300`
  </acceptance_criteria>
  <done>Draft picks trigger a fly-to-roster animation with card clone and burst particles. Pick ticker entries slide in from the right with colored glow.</done>
</task>

<task type="auto">
  <name>Task 2: Wire animated counters to dashboard and add Swamp-o-meter to leaderboard</name>
  <files>src/components/dashboard/kpi-row.tsx, src/components/dashboard/matchup-scoreboard.tsx, src/components/leaderboard/leaderboard-page.tsx, src/components/leaderboard/shame-table.tsx</files>
  <read_first>
    - src/components/dashboard/kpi-row.tsx (current KPI display — find where numeric values are rendered)
    - src/components/dashboard/matchup-scoreboard.tsx (current score display)
    - src/components/leaderboard/leaderboard-page.tsx (leaderboard page structure)
    - src/components/leaderboard/shame-table.tsx (table rows with scores)
    - src/components/animations/animated-counter.tsx (Plan 01 output — AnimatedCounter and DigitFlipCounter exports)
    - src/components/animations/animated-gauge.tsx (Plan 01 output — AnimatedGauge export)
  </read_first>
  <action>
**ANIM-03: Points counters on Dashboard (kpi-row.tsx and matchup-scoreboard.tsx):**

1. **src/components/dashboard/kpi-row.tsx:**
   - Import `{ DigitFlipCounter }` from `'@/components/animations/animated-counter'`
   - Find where numeric KPI values are rendered (season points, league rank number, W-L-T record points)
   - Replace the raw number display for season points with `<DigitFlipCounter value={seasonPoints} className="text-2xl font-bold tabular-nums" />`
   - For league rank, use `<AnimatedCounter value={rank} className="text-2xl font-bold" />` (simpler spring for rank number)
   - Keep text labels (W, L, T, "pts") as static text — only animate the numeric values

2. **src/components/dashboard/matchup-scoreboard.tsx:**
   - Import `{ DigitFlipCounter }` from `'@/components/animations/animated-counter'`
   - Find the score display for user team and opponent team
   - Replace score numbers with `<DigitFlipCounter value={userScore} className="text-3xl font-bold tabular-nums" />` and `<DigitFlipCounter value={opponentScore} className="text-3xl font-bold tabular-nums" />`
   - The digit-flip effect makes the matchup feel like a live sports scoreboard per D-03

**ANIM-05: Swamp-o-meter on leaderboard (leaderboard-page.tsx):**

3. **src/components/leaderboard/leaderboard-page.tsx:**
   - Import `{ AnimatedGauge }` from `'@/components/animations/animated-gauge'`
   - Import `{ motion }` from `'motion/react'`
   - Add a "Swamp-o-meter" section at the top of the corruption tab/section (or as a hero element on the leaderboard page if there's a corruption ranking area)
   - Render: `<motion.div initial={{ opacity: 0 }} whileInView={{ opacity: 1 }} viewport={{ once: true, amount: 0.3 }} className="flex flex-col items-center py-6">` containing:
     - `<h3 className="text-sm font-semibold uppercase tracking-widest text-muted-foreground mb-2">Congressional Swamp-o-Meter</h3>`
     - `<AnimatedGauge score={avgCorruptionScore} size="lg" showLabel labelText="Avg Insider Risk" />` where `avgCorruptionScore` is computed as the mean `insiderRiskScore` across all politicians passed as props
     - `<p className="text-xs text-muted-foreground mt-2">Average Insider Trading Risk Score across {politicians.length} members of Congress</p>`
   - Compute `avgCorruptionScore`: `Math.round(politicians.reduce((sum, p) => sum + p.insiderRiskScore, 0) / politicians.length)`
   - The gauge uses `whileInView` to animate only when scrolled into view

4. **src/components/leaderboard/shame-table.tsx:**
   - For corruption score cells in the table, wrap the score number with `<AnimatedCounter value={politician.insiderRiskScore} className="tabular-nums" />` to make each row's score tick up as the table renders
   - Use staggered `initial={{ opacity: 0, y: 10 }}` and `animate={{ opacity: 1, y: 0 }}` with `transition={{ delay: index * 0.05 }}` on each `<motion.div>` table row for a cascade effect
   - Import `{ motion }` from `'motion/react'` and `{ AnimatedCounter }` from `'@/components/animations/animated-counter'`
  </action>
  <verify>
    <automated>cd /Users/solofilms/FantasyCongress && grep -q "DigitFlipCounter" src/components/dashboard/kpi-row.tsx && grep -q "DigitFlipCounter" src/components/dashboard/matchup-scoreboard.tsx && grep -q "AnimatedGauge" src/components/leaderboard/leaderboard-page.tsx && grep -q "Swamp-o-Meter" src/components/leaderboard/leaderboard-page.tsx && grep -q "AnimatedCounter" src/components/leaderboard/shame-table.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/kpi-row.tsx imports `DigitFlipCounter` from `'@/components/animations/animated-counter'`
    - src/components/dashboard/kpi-row.tsx uses `<DigitFlipCounter` for at least one numeric value
    - src/components/dashboard/matchup-scoreboard.tsx imports `DigitFlipCounter`
    - src/components/dashboard/matchup-scoreboard.tsx uses `<DigitFlipCounter` for both team scores
    - src/components/leaderboard/leaderboard-page.tsx imports `AnimatedGauge` from `'@/components/animations/animated-gauge'`
    - src/components/leaderboard/leaderboard-page.tsx contains text "Swamp-o-Meter"
    - src/components/leaderboard/leaderboard-page.tsx contains `whileInView` for scroll-triggered animation
    - src/components/leaderboard/leaderboard-page.tsx computes avgCorruptionScore from politicians array
    - src/components/leaderboard/shame-table.tsx imports `AnimatedCounter`
    - src/components/leaderboard/shame-table.tsx uses staggered `delay: index *` for row animations
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Dashboard KPIs and matchup scores use digit-flip counters. Leaderboard has a Swamp-o-meter animated gauge at the top of the corruption section with scroll-triggered animation. Shame table rows cascade in with animated scores.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Draft board contains fly animation overlay and burst particles
- Pick ticker entries slide in from right with glow
- Dashboard KPI row uses DigitFlipCounter for numeric values
- Matchup scoreboard uses DigitFlipCounter for both scores
- Leaderboard page contains Swamp-o-meter AnimatedGauge with whileInView
- Shame table rows have staggered animation with AnimatedCounter scores
</verification>

<success_criteria>
- Draft picks visually fly from the pool toward the roster area with a burst effect (ANIM-01)
- Pick ticker entries slide in from the right with colored glow shadows (ANIM-02)
- Dashboard KPI numbers and matchup scores use the digit-flip scoreboard ticker (ANIM-03)
- Leaderboard features a Swamp-o-meter gauge that animates on scroll-into-view (ANIM-05)
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-viral/05-03-SUMMARY.md`
</output>
