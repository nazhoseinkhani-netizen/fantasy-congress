---
phase: 05-polish-and-viral
plan: 05
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - src/app/share/weekly-recap/page.tsx
  - src/components/share/weekly-recap-mockup.tsx
  - src/components/leaderboard/leaderboard-page.tsx
autonomous: false
requirements: [SHARE-04, ANIM-04]

must_haves:
  truths:
    - "The weekly recap email mockup page renders at /share/weekly-recap with team stats, matchup results, and MVP highlights"
    - "The corruption gauge on politician profiles and leaderboard corruption cards animates with the green-to-red sweep on scroll-into-view"
    - "All Phase 5 features build and render without errors"
  artifacts:
    - path: "src/app/share/weekly-recap/page.tsx"
      provides: "Static page for weekly recap email mockup"
    - path: "src/components/share/weekly-recap-mockup.tsx"
      provides: "Email-style layout component with team stats"
  key_links:
    - from: "src/app/share/weekly-recap/page.tsx"
      to: "src/components/share/weekly-recap-mockup.tsx"
      via: "import and render WeeklyRecapMockup"
      pattern: "import.*WeeklyRecapMockup"
---

<objective>
Build the weekly recap email mockup page (SHARE-04, design only), wire the animated corruption gauge into the leaderboard corruption cards (ANIM-04), and verify the complete Phase 5 visually.

Purpose: SHARE-04 is a design-only email mockup showing what a weekly recap would look like. ANIM-04 wires the AnimatedGauge into corruption score displays on the leaderboard. The checkpoint verifies all Phase 5 features work together.

Output: Static /share/weekly-recap page, animated corruption gauges on leaderboard corruption cards, and visual verification of the complete phase.
</objective>

<execution_context>
@/Users/solofilms/.claude/get-shit-done/workflows/execute-plan.md
@/Users/solofilms/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-viral/05-RESEARCH.md
@.planning/phases/05-polish-and-viral/05-01-SUMMARY.md
@.planning/phases/05-polish-and-viral/05-03-SUMMARY.md
@.planning/phases/05-polish-and-viral/05-04-SUMMARY.md

<interfaces>
From src/components/animations/animated-gauge.tsx (Plan 01):
```typescript
export function AnimatedGauge({ score, size, showLabel, labelText, animateOnMount, className }: {
  score: number; size?: 'sm' | 'md' | 'lg'; showLabel?: boolean;
  labelText?: string; animateOnMount?: boolean; className?: string
}): JSX.Element
```

From src/lib/data/demo.ts:
```typescript
export async function loadDemoState(): Promise<DemoState>
```

From src/lib/data/politicians.ts:
```typescript
export async function loadPoliticians(): Promise<Politician[]>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create weekly recap email mockup page and wire corruption gauge into leaderboard</name>
  <files>src/app/share/weekly-recap/page.tsx, src/components/share/weekly-recap-mockup.tsx, src/components/leaderboard/leaderboard-page.tsx</files>
  <read_first>
    - src/components/leaderboard/leaderboard-page.tsx (current leaderboard structure — find corruption ranking section)
    - src/components/leaderboard/shame-table.tsx (corruption score display in table rows)
    - src/lib/data/demo.ts (loadDemoState for recap data)
    - src/lib/data/politicians.ts (loadPoliticians)
    - src/components/animations/animated-gauge.tsx (AnimatedGauge API from Plan 01)
    - src/app/dashboard/page.tsx (example page pattern for data loading)
  </read_first>
  <action>
**SHARE-04: Weekly recap email mockup**

**Create src/components/share/weekly-recap-mockup.tsx:**
- `'use client'` directive
- Import `Politician` type, game store hooks
- Export `WeeklyRecapMockup` with props: `{ teamName: string; weekNumber: number; userScore: number; opponentScore: number; opponentName: string; userRecord: string; mvpPolitician: Politician | null; topTrades: Array<{ politicianName: string; points: number; ticker: string }> }`
- Render an email-style layout with inline styles (like a real email template):
  - Container: `maxWidth: 600, margin: '0 auto', fontFamily: 'system-ui, sans-serif', background: '#0f1729', color: '#e5e7eb', borderRadius: 12, overflow: 'hidden'`
  - Header bar: gold gradient top bar (`height: 4, background: 'linear-gradient(90deg, #d4a843, #b8941f, #d4a843)'`)
  - Logo area: "FANTASY CONGRESS" text centered, `fontSize: 14, letterSpacing: 4, color: '#d4a843', padding: '24px 0 8px'`
  - "WEEKLY RECAP" subtitle, `fontSize: 10, color: '#6b7280', letterSpacing: 3`
  - Week badge: `Week {weekNumber}` pill
  - Matchup section: `<div style={{ background: '#1a1a2e', margin: 16, borderRadius: 8, padding: 20 }}>` with user team vs opponent, scores side by side, win/loss result badge (green "W" or red "L")
  - Record update: "Season Record: {userRecord}"
  - MVP section (if mvpPolitician): photo circle, name, "Week MVP" label, points scored
  - Top Trades list: 3 items with politician name, ticker, points (gold text)
  - Footer: "Powered by Alva" with alva.ai link, `fontSize: 10, color: '#6b7280'`
  - "Manage preferences" mock link (non-functional, just looks real)

**Create src/app/share/weekly-recap/page.tsx:**
- Import `WeeklyRecapMockup`
- This is a static page (no `'use client'` needed if just rendering)
- Actually needs `'use client'` because it loads demo state client-side
- Use `useEffect` + `useState` to load demo data via `loadDemoState()` and `loadPoliticians()`
- Derive: user's team, latest week's matchup, opponent, MVP (highest scoring politician on user's team that week), top 3 trades
- Render page wrapper: `<div className="min-h-screen bg-background py-8 px-4"><div className="max-w-2xl mx-auto"><h1 className="text-lg font-semibold text-muted-foreground mb-4 text-center">Weekly Recap Email — Design Preview</h1><p className="text-xs text-muted-foreground text-center mb-8">This is a mockup of what a weekly recap email would look like. No email infrastructure needed.</p><WeeklyRecapMockup ... /></div></div>`

**ANIM-04: Corruption gauge on leaderboard corruption section**

**Update src/components/leaderboard/leaderboard-page.tsx:**
- Import `{ AnimatedGauge, getTierLabel }` from `'@/components/animations/animated-gauge'` (should already be imported from Plan 03 for Swamp-o-meter — verify and add if not)
- Import `{ motion }` from `'motion/react'` (should already be imported from Plan 03)
- Find the corruption ranking section/tab (the area that shows politician corruption scores, separate from the Swamp-o-meter which is the aggregate gauge)
- For each politician's corruption score display in the corruption ranking list/cards, replace the static score number with:
  ```
  <motion.div whileInView={{ opacity: 1 }} initial={{ opacity: 0 }} viewport={{ once: true, amount: 0.5 }}>
    <AnimatedGauge score={politician.insiderRiskScore} size="sm" showLabel labelText={getTierLabel(politician.insiderRiskScore)} />
  </motion.div>
  ```
- This makes each politician's corruption gauge animate with the green-to-red sweep when scrolled into view (per D-04 theatrical reveal + D-02 load triggers)
- The `whileInView` wrapper ensures gauges only animate when the user scrolls to them (performance optimization, avoids all gauges animating at once off-screen)
  </action>
  <verify>
    <automated>cd /Users/solofilms/FantasyCongress && test -f src/app/share/weekly-recap/page.tsx && test -f src/components/share/weekly-recap-mockup.tsx && grep -q "WeeklyRecapMockup" src/app/share/weekly-recap/page.tsx && grep -q "WEEKLY RECAP" src/components/share/weekly-recap-mockup.tsx && grep -q "Powered by Alva" src/components/share/weekly-recap-mockup.tsx && grep -q "AnimatedGauge" src/components/leaderboard/leaderboard-page.tsx && grep -q "whileInView" src/components/leaderboard/leaderboard-page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/share/weekly-recap/page.tsx exists and imports `WeeklyRecapMockup`
    - src/components/share/weekly-recap-mockup.tsx contains "WEEKLY RECAP" text
    - src/components/share/weekly-recap-mockup.tsx contains "Powered by Alva" text
    - src/components/share/weekly-recap-mockup.tsx contains matchup section with scores
    - src/components/share/weekly-recap-mockup.tsx contains MVP section
    - src/components/share/weekly-recap-mockup.tsx uses inline styles (email-style)
    - src/components/leaderboard/leaderboard-page.tsx uses `AnimatedGauge` for per-politician corruption scores
    - src/components/leaderboard/leaderboard-page.tsx uses `whileInView` for scroll-triggered gauge animation
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Weekly recap email mockup page renders at /share/weekly-recap with dark premium design, matchup results, MVP, and Alva branding. Corruption gauges on leaderboard animate with green-to-red sweep on scroll-into-view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 5</name>
  <action>Human verifies all Phase 5 features work together across the application.</action>
  <what-built>
Complete Phase 5: Polish and Viral
- Animated digit-flip counters on dashboard KPIs and matchup scoreboard
- Animated corruption gauge (green-to-red sweep with overshoot) on leaderboard
- Swamp-o-meter aggregate gauge on leaderboard
- Draft pick fly-to-roster animation with burst effect
- Pick ticker slide-in with colored glow
- Share cards for politicians (dark trading card with Alva branding) and teams
- Share button on leaderboard rows and My Team page header
- Share modal with download, copy link, and native share support
- Developer Mode Easter egg (Ctrl+Shift+D) with dashed borders and tooltips
- AlvaFooter on every page with "Data Sources: Alva Skills API" and "Build Your Own" CTA
- Weekly recap email mockup at /share/weekly-recap
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Visit http://localhost:3000/dashboard
   - Verify KPI numbers animate with digit-flip on page load
   - Verify matchup scoreboard scores animate with digit-flip
   - Verify compact AlvaFooter visible at bottom with "Data Sources" and "Build Your Own" link
3. Visit http://localhost:3000/leaderboard
   - Verify Swamp-o-meter gauge animates when scrolled into view
   - Verify corruption tab shows per-politician gauges that animate on scroll
   - Verify Share button visible on politician rows — tap one to test
   - Verify share card renders dark trading card preview with "Powered by Alva"
   - Verify download works
4. Visit http://localhost:3000/team
   - Verify Share button in header area
   - Verify team share card generates with roster and Alva branding
5. Visit http://localhost:3000/draft
   - Start a draft and make a pick
   - Verify pick card animates (fly effect) with burst particles
   - Verify pick ticker entries slide in from right with glow
   - Verify AlvaFooter is NOT visible during draft (suppressed)
6. Press Ctrl+Shift+D on any page
   - Verify gold banner appears: "Developer Mode Active"
   - Verify dashed gold borders appear on trade cards, KPI row, matchup scoreboard, leaderboard
   - Hover over a bordered element — verify tooltip shows "Alva Skill: getSenatorTrades" (or similar)
   - Press Ctrl+Shift+D again to toggle off
7. Visit http://localhost:3000/share/weekly-recap
   - Verify email mockup renders with dark premium design, matchup results, MVP, and "Powered by Alva"
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All 12 Phase 5 requirements (ANIM-01 through ANIM-05, SHARE-01 through SHARE-04, PLAT-01 through PLAT-03) are addressed
- Every page has AlvaFooter except /draft
- Developer Mode toggles with Ctrl+Shift+D
- Share cards generate as downloadable PNGs with Alva branding
- All animations use Motion library (no hand-rolled spring/intersection logic)
</verification>

<success_criteria>
- Weekly recap mockup renders at /share/weekly-recap with email-style design (SHARE-04)
- Corruption gauges animate with green-to-red sweep on leaderboard corruption cards (ANIM-04)
- Human verification confirms all Phase 5 features work together
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-viral/05-05-SUMMARY.md`
</output>
