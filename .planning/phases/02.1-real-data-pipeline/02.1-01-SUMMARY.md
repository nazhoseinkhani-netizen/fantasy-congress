---
phase: 02.1-real-data-pipeline
plan: 01
subsystem: api
tags: [alva, fetch, congressional-trades, data-pipeline, ohlcv, typescript]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: RawTrade interface, score-all.ts scoring consumer, TICKERS static map, build pipeline pattern
  - phase: 02-discovery
    provides: UI pages that consume public/data/*.json in existing shape

provides:
  - scripts/fetch-trades.ts rewritten to call Alva REST API for real Congressional stock disclosures
  - callAlvaScript() helper: POST /api/v1/run with X-Alva-Api-Key, retry on 429/5xx
  - SDK partition discovery (writes _alva-sdk-discovery.json for debugging)
  - Politician name normalization + lookup (join to _raw-politicians.json by name)
  - Stock price fetch via @arrays/data/stock/ohlcv:v1.0.0 for return calculations
  - mapAmountRange(): parses Alva amount strings to existing AmountRange union
  - normalizeName(): strips honorifics, handles "Last, First" format
  - Concurrent price fetching (5 workers, Promise.all semaphore)
  - Zero simulated trade generation code remains

affects: [02.1-02-generate-demo, score-all, generate-demo, public/data/_raw-trades.json]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Alva V8 sandbox: require('@arrays/...') calls go inside code strings passed to POST /api/v1/run — not in Node.js scope"
    - "Defensive field mapping: extractField() tries multiple Alva field name variants (transactionDate/transaction_date/tradeDate)"
    - "Concurrent API calls: 5-worker semaphore via Promise.all + shared queue array"
    - "Build-fail-loud pattern: process.exit(1) on ALVA_API_KEY missing, 0 trades, <10 matched trades"
    - "Name normalization: strip honorifics + reverse 'Last, First' + lowercase for politician lookup"

key-files:
  created: []
  modified:
    - scripts/fetch-trades.ts

key-decisions:
  - "Defensive Alva response unwrapping: try resp.result, resp.output, then raw — field name uncertain until live API run"
  - "Last-name-only fallback for politician matching when full name normalization fails (only when unambiguous)"
  - "Set price returns to 0 (not null) for missing price data — score-all.ts excludes 0-return trades from scoring per D-06"
  - "Unknown tickers fall back to sector='Other', company=ticker symbol — no extra Alva fundamentals call per RESEARCH.md recommendation"
  - "SPY added to ticker list for S&P 500 benchmark; per-trade calculation from tradeDate to min(tradeDate+365d, today)"

patterns-established:
  - "Alva API pattern: fetch() to POST /api/v1/run, parse result field, handle multiple container shapes (Array, .trades, .data, .results)"
  - "OHLCV price lookup: find closest bar within 14 calendar days of target date"

requirements-completed: [DATA-01, DATA-02, DATA-03, DATA-05]

# Metrics
duration: 4min
completed: 2026-03-23
---

# Phase 02.1 Plan 01: Real Trade Pipeline Summary

**Alva REST API integration replacing simulated RNG trades: callAlvaScript() POSTs senator-trades and OHLCV scripts to /api/v1/run with defensive field mapping and concurrent price lookups**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-23T19:59:16Z
- **Completed:** 2026-03-23T20:03:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Replaced 267 lines of simulated trade generation (mulberry32 RNG, normalRandom, weightedChoice, generateTradesForPolitician, getPreferredSectors, tryQuiverAPI) with 782 lines of real Alva API integration
- Implemented full pipeline: SDK discovery -> fetch senator trades -> politician name matching -> stock price lookups (5 concurrent workers) -> return calculations -> TICKERS enrichment -> write _raw-trades.json
- Build now fails loudly on missing ALVA_API_KEY, 0 Alva trades returned, or <10 matched politician trades (per D-04)

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite fetch-trades.ts with Alva API integration and mapping layer** - `3bd727d` (feat)

## Files Created/Modified

- `scripts/fetch-trades.ts` - Complete rewrite: Alva API calls, politician name normalization, OHLCV price lookup, mapAmountRange, RawTrade mapping layer

## Decisions Made

- Defensive Alva response unwrapping tries `resp.result`, `resp.output`, then raw response — exact field name unknown until live API run
- Last-name-only fallback for politician matching when full normalized name fails, only used when last name is unambiguous
- Set returns to 0 (not null/NaN) for missing price data — score-all.ts already excludes 0-return trades per D-06
- Unknown tickers get `company=ticker`, `sector="Other"` — no extra Alva fundamentals call per research recommendation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

TypeScript Set spread `[...set]` triggered errors when running `tsc` standalone without project tsconfig. Project tsconfig uses `"lib": ["esnext"]` which includes Set iterability — no actual issue; `npx tsc --noEmit` (full project compilation) passes cleanly.

## User Setup Required

**ALVA_API_KEY must be set before running the build pipeline.** The script now exits with an error message if this environment variable is missing:

```
[fetch-trades] FATAL: ALVA_API_KEY not set — build cannot proceed. Set ALVA_API_KEY in .env or environment.
```

## Next Phase Readiness

- `scripts/fetch-trades.ts` is ready to run with a valid `ALVA_API_KEY`
- The script will discover actual Alva SDK module names on first run (via `_alva-sdk-discovery.json`)
- Downstream scripts (score-all.ts, validate-photos.ts, generate-demo.ts) are unchanged — they consume `_raw-trades.json` in the same shape
- Plan 02: generate-demo.ts season window update (D-03: most recent 3 months of real data) is the next dependency

---
*Phase: 02.1-real-data-pipeline*
*Completed: 2026-03-23*
