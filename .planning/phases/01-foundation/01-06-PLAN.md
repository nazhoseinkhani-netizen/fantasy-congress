---
phase: 01-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scoring/engine.ts
  - src/lib/scoring/__tests__/engine.test.ts
  - scripts/score-all.ts
  - public/data/politicians.json
  - public/data/trades.json
  - public/data/build-report.json
autonomous: true
gap_closure: true
requirements:
  - SCORE-03

must_haves:
  truths:
    - "scorePolitician() adds activityBonus (config.bonuses.activityBonus * trades.length) to seasonPoints"
    - "Unit test explicitly asserts activityBonus is included in scorePolitician() output"
    - "All politicians in public/data/politicians.json have seasonPoints that include the activity bonus component"
  artifacts:
    - path: "src/lib/scoring/engine.ts"
      provides: "Activity bonus computation in scorePolitician()"
      contains: "activityBonus"
    - path: "src/lib/scoring/__tests__/engine.test.ts"
      provides: "Test coverage for activity bonus in scorePolitician"
      contains: "activityBonus"
    - path: "public/data/politicians.json"
      provides: "Regenerated politician data with corrected seasonPoints"
  key_links:
    - from: "src/lib/scoring/engine.ts"
      to: "src/lib/scoring/config.ts"
      via: "config.bonuses.activityBonus"
      pattern: "config\\.bonuses\\.activityBonus"
---

<objective>
Fix the Activity Bonus scoring gap (SCORE-03): the +5/trade activity bonus is defined in ScoringConfig but never applied by scorePolitician(). Add the computation, add a unit test, and regenerate all pipeline data.

Purpose: All 82 politicians' seasonPoints are understated by 5 * tradeCount. High-volume traders are most affected. This blocks SCORE-03 requirement satisfaction.
Output: Corrected engine.ts, new test case, regenerated public/data/*.json files.
</objective>

<execution_context>
@/Users/solofilms/.claude/get-shit-done/workflows/execute-plan.md
@/Users/solofilms/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/scoring/config.ts:
```typescript
export const DEFAULT_SCORING_CONFIG: ScoringConfig = {
  // ...
  bonuses: {
    insiderTiming: 15,
    donorDarling: 10,
    bigMover: 20,
    bipartisanBet: 25,
    activityBonus: 5,   // <-- THIS VALUE IS NEVER USED
  },
  // ...
}
```

From src/types/scoring.ts:
```typescript
export interface PoliticianScore {
  bioguideId: string
  seasonPoints: number
  weeklyPoints: number[]
  tradeScores: TradeScore[]
  tradeCount: number
  winRate: number
  avgReturn: number
}
```

From src/lib/scoring/engine.ts (current scorePolitician, line 108):
```typescript
// Season total — THIS LINE IS THE BUG: missing activityBonus
const seasonPoints = tradeScores.reduce((sum, s) => sum + s.total, 0)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add activity bonus to scorePolitician() with TDD</name>
  <files>src/lib/scoring/engine.ts, src/lib/scoring/__tests__/engine.test.ts</files>
  <read_first>
    - src/lib/scoring/engine.ts (the file being modified — see scorePolitician() at line 85-141)
    - src/lib/scoring/__tests__/engine.test.ts (existing test file — add new test case)
    - src/lib/scoring/config.ts (DEFAULT_SCORING_CONFIG with activityBonus: 5)
    - src/types/scoring.ts (PoliticianScore interface)
  </read_first>
  <behavior>
    - Test: scorePolitician() with 3 trades should include activityBonus = 3 * 5 = 15 in seasonPoints
    - Test: scorePolitician() seasonPoints should equal sum of trade scores PLUS (tradeCount * config.bonuses.activityBonus)
    - Test: scorePolitician() with 0 trades should return seasonPoints = 0 (no activity bonus for empty array)
  </behavior>
  <action>
    RED: Add a test case in engine.test.ts within the existing `describe('scorePolitician')` block. The test should:
    - Create 3 trades using the existing baseTrade fixture pattern
    - Call scorePolitician(trades, context, config) where config uses DEFAULT_SCORING_CONFIG (activityBonus: 5)
    - Assert that result.seasonPoints equals the sum of individual trade totals PLUS (3 * 5 = 15)
    - Run `npx vitest run src/lib/scoring/__tests__/engine.test.ts` — test MUST FAIL (seasonPoints will be 15 less than expected)

    GREEN: In src/lib/scoring/engine.ts, modify scorePolitician() to add the activity bonus. Change line 108 from:
    ```
    const seasonPoints = tradeScores.reduce((sum, s) => sum + s.total, 0)
    ```
    to:
    ```
    const tradeTotal = tradeScores.reduce((sum, s) => sum + s.total, 0)
    const activityBonus = trades.length * config.bonuses.activityBonus
    const seasonPoints = tradeTotal + activityBonus
    ```
    Run `npx vitest run src/lib/scoring/__tests__/engine.test.ts` — ALL tests MUST PASS

    Per STATE.md decision: "activityBonus applied at politician level in scorePolitician(), not per-trade in detectBonuses()"
  </action>
  <verify>
    <automated>npx vitest run src/lib/scoring/__tests__/engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "activityBonus" src/lib/scoring/engine.ts returns at least 2 (variable declaration + usage)
    - grep -c "activityBonus" src/lib/scoring/__tests__/engine.test.ts returns at least 1 (new test)
    - npx vitest run src/lib/scoring/__tests__/engine.test.ts exits with code 0
    - The seasonPoints computation in engine.ts includes `config.bonuses.activityBonus`
  </acceptance_criteria>
  <done>scorePolitician() adds activityBonus * tradeCount to seasonPoints. New test proves it. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate pipeline data with corrected scoring</name>
  <files>public/data/politicians.json, public/data/trades.json, public/data/build-report.json, public/data/leagues.json, public/data/matchups.json</files>
  <read_first>
    - src/lib/scoring/engine.ts (confirm activityBonus is now applied from Task 1)
    - scripts/build-pipeline.ts (understand the 5-step pipeline sequence)
    - scripts/score-all.ts (confirm it calls scorePolitician which now includes activityBonus)
  </read_first>
  <action>
    Run the full build pipeline to regenerate all JSON data with corrected activity bonus scoring:

    ```
    npm run fetch-data
    ```

    This runs scripts/build-pipeline.ts which executes all 5 pipeline steps in order:
    1. fetch-politicians (reads existing _validated-politicians.json)
    2. fetch-trades (reads existing _raw-trades.json)
    3. validate-photos (validates photo URLs)
    4. score-all (NOW applies activityBonus via corrected scorePolitician())
    5. generate-demo (regenerates leagues/matchups from updated politician scores)

    After pipeline completes, verify:
    - Pick a politician with known trade count (e.g., first entry in politicians.json)
    - Sum their trade fantasyPoints from trades.json
    - Confirm politicians.json seasonPoints = tradeSum + (tradeCount * 5)

    Note: This also regenerates leagues.json and matchups.json, which will have updated scores but the bench-fill issue may still be present (that is addressed in plan 01-07).
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('public/data/politicians.json','utf8')); const t=JSON.parse(require('fs').readFileSync('public/data/trades.json','utf8')); const pol=p[0]; const trades=t.filter(tr=>tr.bioguideId===pol.bioguideId); const tradeSum=trades.reduce((s,tr)=>s+tr.fantasyPoints,0); const expected=tradeSum+(trades.length*5); console.log('politician:',pol.name,'trades:',trades.length,'tradeSum:',tradeSum.toFixed(2),'expected:',expected.toFixed(2),'actual:',pol.seasonPoints.toFixed(2),'match:',Math.abs(pol.seasonPoints-expected)<0.01); if(Math.abs(pol.seasonPoints-expected)>=0.01) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - public/data/politicians.json exists and contains 80+ politicians
    - For any politician: seasonPoints equals (sum of their trade fantasyPoints) + (tradeCount * 5)
    - public/data/trades.json exists and contains 800+ trades
    - npm run fetch-data exits with code 0
  </acceptance_criteria>
  <done>All public/data/*.json files regenerated with corrected seasonPoints that include the activity bonus. Pipeline runs cleanly.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/scoring/__tests__/engine.test.ts` — all tests pass including new activityBonus test
2. Spot-check any politician: seasonPoints = sum(trade fantasyPoints) + (tradeCount * 5)
3. grep confirms `activityBonus` appears in engine.ts computation
</verification>

<success_criteria>
- SCORE-03 requirement satisfied: Activity Bonus (+5/trade) is computed and included in all politician scores
- Unit test proves the activity bonus is applied at the politician level
- All 82 politicians in the dataset have corrected seasonPoints
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
