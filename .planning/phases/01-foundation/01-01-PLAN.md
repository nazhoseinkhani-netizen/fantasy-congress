---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - src/styles/globals.css
  - src/types/politician.ts
  - src/types/trade.ts
  - src/types/scoring.ts
  - src/types/demo.ts
  - src/types/index.ts
  - src/lib/scoring/config.ts
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-05
  - SCORE-06

must_haves:
  truths:
    - "Next.js 16 project bootstrapped with TypeScript, Tailwind CSS 4, and App Router"
    - "shadcn/ui initialized with new-york style and dark mode default"
    - "All TypeScript interfaces for the entire data layer are defined and exported"
    - "Scoring config type and default values are defined as a standalone module"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "next.config.ts"
      provides: "Static export configuration"
      contains: "output"
    - path: "src/types/politician.ts"
      provides: "Politician data type"
      exports: ["Politician", "InsiderRiskTier", "Party", "Chamber"]
    - path: "src/types/trade.ts"
      provides: "Trade data type"
      exports: ["Trade", "TradeType", "AmountRange"]
    - path: "src/types/scoring.ts"
      provides: "Scoring types"
      exports: ["ScoringConfig", "TradeScore", "PoliticianScore", "BonusType", "PenaltyType", "SeasonWeek"]
    - path: "src/types/demo.ts"
      provides: "Demo data types"
      exports: ["League", "Team", "Matchup", "Roster", "WeekResult"]
    - path: "src/lib/scoring/config.ts"
      provides: "Default scoring configuration"
      exports: ["DEFAULT_SCORING_CONFIG", "SEASON_WEEKS"]
  key_links:
    - from: "src/types/index.ts"
      to: "src/types/*.ts"
      via: "barrel re-exports"
      pattern: "export.*from"
    - from: "src/lib/scoring/config.ts"
      to: "src/types/scoring.ts"
      via: "imports ScoringConfig type"
      pattern: "import.*ScoringConfig.*from"
---

<objective>
Bootstrap the Next.js 16 project with all dependencies and define every TypeScript interface for the data layer, scoring engine, and demo data. This plan creates the contracts that all subsequent plans implement against.

Purpose: Downstream plans (scoring engine, data pipeline, design system, demo data) need stable type definitions to implement against without guessing. Project bootstrap must happen first since every other plan builds on the Next.js project structure.

Output: Working Next.js 16 project with shadcn/ui initialized, all TypeScript types exported, and scoring config defined.
</objective>

<execution_context>
@/Users/solofilms/.claude/get-shit-done/workflows/execute-plan.md
@/Users/solofilms/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js 16 project with all dependencies</name>
  <files>package.json, next.config.ts, tsconfig.json, src/app/layout.tsx, src/styles/globals.css</files>
  <read_first>
    - /Users/solofilms/FantasyCongress/CLAUDE.md (technology stack, installation commands, what NOT to use)
    - /Users/solofilms/FantasyCongress/.planning/phases/01-foundation/01-RESEARCH.md (installation section, Pattern 4 CSS variables, anti-patterns)
  </read_first>
  <action>
    1. Run project bootstrap from the FantasyCongress directory:
       ```
       npx create-next-app@16 . --typescript --tailwind --eslint --app --src-dir --import-alias="@/*"
       ```
       If the directory is not empty, use a temp directory and move files, or answer the overwrite prompt.

    2. Install all dependencies:
       ```
       npm install zustand @tanstack/react-query motion date-fns next-themes
       npm install --save-dev tsx
       ```

    3. Initialize shadcn/ui:
       ```
       npx shadcn@latest init
       ```
       Select: New York style, dark mode default. If prompted for CSS variables, select yes.

    4. Configure next.config.ts for static export:
       ```typescript
       import type { NextConfig } from 'next'

       const nextConfig: NextConfig = {
         output: 'export',
         images: { unoptimized: true },
       }

       export default nextConfig
       ```

    5. Add build pipeline scripts to package.json:
       ```json
       "scripts": {
         "prebuild": "npx tsx scripts/build-pipeline.ts",
         "build": "next build",
         "fetch-data": "npx tsx scripts/build-pipeline.ts",
         "dev": "next dev --turbopack",
         "lint": "next lint"
       }
       ```

    6. Create the scripts/ directory and a placeholder build-pipeline.ts:
       ```typescript
       // scripts/build-pipeline.ts
       async function main() {
         console.log('Build pipeline placeholder — implemented in Plan 04')
       }
       main().catch((err) => { console.error(err); process.exit(1) })
       ```

    7. Create public/data/ directory with a .gitkeep file.

    8. Ensure src/styles/globals.css has the Tailwind imports. The full dark theme CSS variables will be set up in Plan 03 (Design System). For now ensure the file has:
       ```css
       @import "tailwindcss";
       ```

    9. Update src/app/layout.tsx to wrap with ThemeProvider from next-themes:
       ```typescript
       import { ThemeProvider } from 'next-themes'
       ```
       Set `<html suppressHydrationWarning>` to prevent hydration mismatch.
       Wrap children in `<ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}>`.
  </action>
  <verify>
    <automated>cd /Users/solofilms/FantasyCongress && npm run dev -- --port 3099 &amp; sleep 8 && curl -s -o /dev/null -w "%{http_code}" http://localhost:3099 && kill %1</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" in dependencies
    - package.json contains "zustand" in dependencies
    - package.json contains "motion" in dependencies
    - package.json contains "date-fns" in dependencies
    - package.json contains "next-themes" in dependencies
    - package.json contains "tsx" in devDependencies
    - package.json contains "prebuild" in scripts
    - next.config.ts contains `output: 'export'`
    - next.config.ts contains `images: { unoptimized: true }`
    - src/app/layout.tsx contains "ThemeProvider"
    - src/app/layout.tsx contains "suppressHydrationWarning"
    - src/app/layout.tsx contains `defaultTheme="dark"`
    - public/data/ directory exists
    - scripts/build-pipeline.ts exists
    - `npm run dev` starts without errors (dev server responds 200)
  </acceptance_criteria>
  <done>Next.js 16 project runs in dev mode with all dependencies installed, static export configured, shadcn/ui initialized, and ThemeProvider wrapping the app in dark mode.</done>
</task>

<task type="auto">
  <name>Task 2: Define all TypeScript interfaces and scoring config</name>
  <files>src/types/politician.ts, src/types/trade.ts, src/types/scoring.ts, src/types/demo.ts, src/types/index.ts, src/lib/scoring/config.ts</files>
  <read_first>
    - /Users/solofilms/FantasyCongress/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 scoring config, Pattern 3 data types)
    - /Users/solofilms/FantasyCongress/.planning/REQUIREMENTS.md (DATA-02, DATA-03, DATA-05, SCORE-01 through SCORE-06, CORR-01 through CORR-03, DEMO-01 through DEMO-04)
    - /Users/solofilms/FantasyCongress/.planning/phases/01-foundation/01-CONTEXT.md (decisions D-04, D-05, D-08, D-12)
  </read_first>
  <action>
    1. Create src/types/politician.ts:
       ```typescript
       export type Party = 'D' | 'R' | 'I'
       export type Chamber = 'senate' | 'house'

       export type InsiderRiskTier =
         | 'clean-record'          // 0-14
         | 'minor-concerns'        // 15-34
         | 'raised-eyebrows'       // 35-59
         | 'seriously-suspicious'  // 60-84
         | 'peak-swamp'            // 85-100

       export interface InsiderRiskBreakdown {
         donorOverlap: number       // 0-100 component
         suspiciousTiming: number   // 0-100 component
         committeeConflict: number  // 0-100 component
         stockActCompliance: number // 0-100 component (inverted: lower = worse)
         tradeVolume: number        // 0-100 component
       }

       export interface Politician {
         bioguideId: string
         name: string
         firstName: string
         lastName: string
         party: Party
         chamber: Chamber
         state: string
         district?: string           // House members only
         committees: string[]
         photoUrl: string            // Pre-validated working URL or data URI
         isCommitteeChair: boolean
         isLeadership: boolean
         seasonPoints: number
         weeklyPoints: number[]      // Points per week (6 weeks)
         tradeCount: number
         winRate: number             // 0-1 (fraction of trades that beat S&P)
         avgReturn: number           // Average % return across trades
         insiderRiskScore: number    // 0-100 composite
         insiderRiskTier: InsiderRiskTier
         insiderRiskBreakdown: InsiderRiskBreakdown
         salaryCap: number           // Fantasy salary cost
         salaryTier: SalaryTier
       }

       export type SalaryTier = 'elite' | 'starter' | 'mid-tier' | 'bench' | 'sleeper' | 'unranked'
       ```

    2. Create src/types/trade.ts:
       ```typescript
       import type { Party, Chamber } from './politician'

       export type TradeType = 'buy' | 'sell'

       export type AmountRange =
         | '$1k-$15k'
         | '$15k-$50k'
         | '$50k-$100k'
         | '$100k-$250k'
         | '$250k-$500k'
         | '$500k-$1M'
         | '$1M+'

       export interface Trade {
         id: string
         bioguideId: string
         politicianName: string
         party: Party
         chamber: Chamber
         ticker: string
         company: string
         sector: string
         tradeType: TradeType
         disclosureDate: string     // ISO 8601 date string
         tradeDate: string          // ISO 8601 date string
         amountRange: AmountRange
         returnVsSP500: number      // % return minus S&P return over same period
         absoluteReturn: number     // % absolute return
         sp500Return: number        // % S&P 500 return over same period
         daysToDisclose: number     // Days between trade and disclosure
         fantasyPoints: number      // Pre-computed total fantasy points
         scoreBreakdown: TradeScoreBreakdown
       }

       export interface TradeScoreBreakdown {
         basePoints: number
         excessReturnPoints: number
         amountMultiplier: number
         bonuses: { type: string; points: number }[]
         penalties: { type: string; points: number }[]
         positionMultiplier: number   // Committee chair / leadership
         totalBeforeMultiplier: number
         total: number
       }
       ```

    3. Create src/types/scoring.ts:
       ```typescript
       export interface ScoringConfig {
         basePointsOutperform: number
         basePointsUnderperform: number
         excessReturnMultiplier: number
         amountMultipliers: {
           '$1k-$15k': number
           '$15k-$50k': number
           '$50k-$100k': number
           '$100k-$250k': number
           '$250k-$500k': number
           '$500k-$1M': number
           '$1M+': number
         }
         bonuses: {
           insiderTiming: number     // +15: traded within 7 days of committee hearing on related sector
           donorDarling: number      // +10: politician received campaign donations from traded company
           bigMover: number          // +20: trade return exceeded 30% absolute
           bipartisanBet: number     // +25: politician traded against their party's legislative position
           activityBonus: number     // +5 per trade in scoring period
         }
         penalties: {
           paperHands: number        // -15: sold within 14 days of buying same ticker
           lateDisclosure: number    // -10: disclosed more than 45 days after trade (STOCK Act violation)
           washSale: number          // -5: bought and sold same ticker within 30 days
         }
         multipliers: {
           committeeChair: number    // 1.5x
           leadership: number        // 1.3x
         }
       }

       export type BonusType = 'insiderTiming' | 'donorDarling' | 'bigMover' | 'bipartisanBet' | 'activityBonus'
       export type PenaltyType = 'paperHands' | 'lateDisclosure' | 'washSale'

       export interface TradeScore {
         tradeId: string
         basePoints: number
         excessReturnPoints: number
         amountMultiplier: number
         bonuses: { type: BonusType; points: number }[]
         penalties: { type: PenaltyType; points: number }[]
         positionMultiplier: number
         totalBeforeMultiplier: number
         total: number
       }

       export interface PoliticianScore {
         bioguideId: string
         seasonPoints: number
         weeklyPoints: number[]
         tradeScores: TradeScore[]
         tradeCount: number
         winRate: number
         avgReturn: number
       }

       export interface InsiderRiskConfig {
         weights: {
           donorOverlap: number       // e.g. 0.20
           suspiciousTiming: number    // e.g. 0.30
           committeeConflict: number   // e.g. 0.25
           stockActCompliance: number  // e.g. 0.15
           tradeVolume: number         // e.g. 0.10
         }
         tierThresholds: {
           cleanRecord: number         // 0-14
           minorConcerns: number       // 15-34
           raisedEyebrows: number      // 35-59
           seriouslySuspicious: number // 60-84
           // 85-100 is Peak Swamp (implicit)
         }
       }

       /** Defines a scoring week boundary for bucketing trades into weekly points. */
       export interface SeasonWeek {
         week: number            // 1-6
         startDate: string       // ISO 8601 date (inclusive)
         endDate: string         // ISO 8601 date (inclusive)
       }
       ```

    4. Create src/types/demo.ts:
       ```typescript
       export interface League {
         id: string
         name: string
         teams: Team[]
         schedule: WeekSchedule[]
         currentWeek: number          // 1-6
       }

       export interface Team {
         id: string
         leagueId: string
         name: string
         owner: string                // Display name
         isUserTeam: boolean
         roster: Roster
         record: { wins: number; losses: number; ties: number }
         pointsFor: number
         pointsAgainst: number
         streak: string               // e.g. "W2", "L1", "T1"
       }

       export interface Roster {
         active: string[]             // bioguideIds (8 slots)
         bench: string[]              // bioguideIds (4 slots)
         salaryCap: number            // Total cap (e.g. 50000)
         salaryUsed: number           // Sum of active + bench salaries
       }

       export interface WeekSchedule {
         week: number
         matchups: Matchup[]
       }

       export interface Matchup {
         week: number
         leagueId: string
         homeTeamId: string
         awayTeamId: string
         homeScore: number
         awayScore: number
         completed: boolean
       }

       export interface WeekResult {
         week: number
         teamId: string
         points: number
         politicianPoints: { bioguideId: string; points: number }[]
         mvpBioguideId: string
       }

       export interface DemoState {
         activeLeagueId: string
         userTeamId: string
         leagues: League[]
         weekResults: WeekResult[]
       }
       ```

    5. Create src/types/index.ts barrel export:
       ```typescript
       export * from './politician'
       export * from './trade'
       export * from './scoring'
       export * from './demo'
       ```

    6. Create src/lib/scoring/config.ts with the default scoring configuration:
       ```typescript
       import type { ScoringConfig, InsiderRiskConfig, SeasonWeek } from '@/types/scoring'

       export const DEFAULT_SCORING_CONFIG: ScoringConfig = {
         basePointsOutperform: 10,
         basePointsUnderperform: -5,
         excessReturnMultiplier: 2,
         amountMultipliers: {
           '$1k-$15k': 1,
           '$15k-$50k': 1.5,
           '$50k-$100k': 2,
           '$100k-$250k': 2.5,
           '$250k-$500k': 3,
           '$500k-$1M': 3.5,
           '$1M+': 4,
         },
         bonuses: {
           insiderTiming: 15,
           donorDarling: 10,
           bigMover: 20,
           bipartisanBet: 25,
           activityBonus: 5,
         },
         penalties: {
           paperHands: -15,
           lateDisclosure: -10,
           washSale: -5,
         },
         multipliers: {
           committeeChair: 1.5,
           leadership: 1.3,
         },
       }

       export const DEFAULT_INSIDER_RISK_CONFIG: InsiderRiskConfig = {
         weights: {
           donorOverlap: 0.20,
           suspiciousTiming: 0.30,
           committeeConflict: 0.25,
           stockActCompliance: 0.15,
           tradeVolume: 0.10,
         },
         tierThresholds: {
           cleanRecord: 15,
           minorConcerns: 35,
           raisedEyebrows: 60,
           seriouslySuspicious: 85,
         },
       }

       /** Season week boundaries — 6 weeks covering the demo season. */
       /** Trades are bucketed into weeks by comparing trade.tradeDate against these date ranges. */
       /** A trade falls into the week where startDate <= tradeDate <= endDate. */
       /** Trades outside all week ranges are excluded from weeklyPoints but still count toward seasonPoints. */

       export const SEASON_WEEKS: SeasonWeek[] = [
         { week: 1, startDate: '2025-10-01', endDate: '2025-10-14' },
         { week: 2, startDate: '2025-10-15', endDate: '2025-10-28' },
         { week: 3, startDate: '2025-10-29', endDate: '2025-11-11' },
         { week: 4, startDate: '2025-11-12', endDate: '2025-11-25' },
         { week: 5, startDate: '2025-11-26', endDate: '2025-12-09' },
         { week: 6, startDate: '2025-12-10', endDate: '2025-12-23' },
       ]
       ```

    7. Verify all types compile: run `npx tsc --noEmit` from project root.
  </action>
  <verify>
    <automated>cd /Users/solofilms/FantasyCongress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/types/politician.ts contains `export interface Politician`
    - src/types/politician.ts contains `export type InsiderRiskTier`
    - src/types/politician.ts contains `export type Party = 'D' | 'R' | 'I'`
    - src/types/politician.ts contains `export type SalaryTier`
    - src/types/politician.ts contains `export interface InsiderRiskBreakdown`
    - src/types/trade.ts contains `export interface Trade`
    - src/types/trade.ts contains `export type TradeType = 'buy' | 'sell'`
    - src/types/trade.ts contains `export type AmountRange`
    - src/types/trade.ts contains `export interface TradeScoreBreakdown`
    - src/types/scoring.ts contains `export interface ScoringConfig`
    - src/types/scoring.ts contains `export interface TradeScore`
    - src/types/scoring.ts contains `export interface PoliticianScore`
    - src/types/scoring.ts contains `export interface InsiderRiskConfig`
    - src/types/scoring.ts contains `export type BonusType`
    - src/types/scoring.ts contains `export type PenaltyType`
    - src/types/demo.ts contains `export interface League`
    - src/types/demo.ts contains `export interface Team`
    - src/types/demo.ts contains `export interface Matchup`
    - src/types/demo.ts contains `export interface Roster`
    - src/types/demo.ts contains `export interface DemoState`
    - src/types/index.ts contains `export * from './politician'`
    - src/lib/scoring/config.ts contains `export const DEFAULT_SCORING_CONFIG`
    - src/lib/scoring/config.ts contains `export const DEFAULT_INSIDER_RISK_CONFIG`
    - src/lib/scoring/config.ts contains `import type { ScoringConfig, InsiderRiskConfig, SeasonWeek } from '@/types/scoring'`
    - src/types/scoring.ts contains `export interface SeasonWeek`
    - src/lib/scoring/config.ts contains `export const SEASON_WEEKS`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All TypeScript interfaces for politicians, trades, scoring, and demo data are defined, exported via barrel file, and compile without errors. Scoring config with all point values, multipliers, bonuses, and penalties is defined as a configurable object.</done>
</task>

</tasks>

<verification>
- `npm run dev` starts the dev server without errors
- `npx tsc --noEmit` compiles all types without errors
- All type files exist under src/types/ with correct exports
- src/lib/scoring/config.ts exports DEFAULT_SCORING_CONFIG and DEFAULT_INSIDER_RISK_CONFIG
- next.config.ts has static export and unoptimized images
- package.json has all required dependencies
</verification>

<success_criteria>
Working Next.js 16 project with complete TypeScript type system for the entire Fantasy Congress data layer. All downstream plans can import types from @/types and scoring config from @/lib/scoring/config without any additional type definition work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
